import React from 'react';
import AppNavigator from '../navigation/AppNavigator'; // Import the AppNavigator component

export default function Index() {
  return <AppNavigator />; // Use the AppNavigator as the main entry point
}
